{
  "name": "Linux x64 DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--net=host"],
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "ms-python.python",
        "ms-azuretools.vscode-docker",
        "rust-lang.rust-analyzer"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'uv pip install --system -r requirements.txt; cargo --version; rustc --version; protoc --version; echo DevContainer setup complete'"
}
